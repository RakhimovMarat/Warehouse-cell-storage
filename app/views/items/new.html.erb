<h1>New part</h1>

<%= form_for :item, url: items_path, data: { turbo: false } do |f| %>
	<p>
		<%= f.label :number %> <br />
		<%= f.text_field :number %>
	</p>

	<p>
		<%= f.label :description %> <br />
		<%= f.text_area :description %>
	</p>

	<p>
		<%= f.submit 'Create part' %>
	</p>

<% end %>